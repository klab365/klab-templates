# Bacon configuration file
# https://dystroy.org/bacon/

default_job = "check"

[jobs.check]
command = ["cargo", "check", "--color", "always"]
need_stdout = false


[keybindings]
# Press 'c' for check, 't' for test, 'r' for run, 'l' for clippy, 'd' for doc
c = "job:check"
